services:
  db_test:
    image: postgres:16-alpine
    container_name: vel_iam_db_test
    environment:
      POSTGRES_USER: db_test_user
      POSTGRES_PASSWORD: db_test_pass
      POSTGRES_DB: db_test
    ports:
      - "5434:5432"
    tmpfs:
      - /var/lib/postgresql/data
